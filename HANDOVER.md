# M3DStore — ملف التسليم

**التاريخ**: 19 يوليو 2026  
**الحالة**: جاهز للاستخدام والنشر

---

## 📋 ملخص المشروع

متجر إلكتروني عربي كامل (RTL) لبيع منتجات الطباعة ثلاثية الأبعاد. مبني بـ **Next.js 16 + Tailwind + SQLite**.

**المميزات الرئيسية**:
- عرض منتجات + سلة + دفع (تجريبي، جاهز للربط مع Moyasar أو Tap)
- طلبات تصاميم مخصصة (العميل يرفع ملف + يشرح الفكرة، أنت توافق بسعر قبل الدفع)
- لوحة تحكم محمية بكلمة مرور (إدارة منتجات، طلبات، تصاميم مخصصة)
- تتبع الطلبات برمز فريد
- واتساب عائم لكل الصفحات
- دعم 5 وسائل دفع (مدى، Apple Pay، STC Pay، تابي، تمارا) — حاليًا تجريبي

---

## 🌐 الصفحات الرئيسية

| الرابط | الوصف | ملاحظات |
|---|---|---|
| `/` | الرئيسية | بطل، مزايا، 4 منتجات مميزة، خطوات الطلب المخصص |
| `/products` | قائمة المنتجات | تصفية بالفئات + بحث + 8 منتجات تجريبية |
| `/products/[id]` | صفحة المنتج | صورة + وصف + أضف للسلة / اشترِ الآن + منتجات مشابهة |
| `/custom` | طلب تصميم مخصص | اسم + جوال + شرح + **رفع ملف (إجباري الآن)** |
| `/cart` | سلة المشتريات | عرض المنتجات + كميات + حساب الشحن |
| `/checkout` | إتمام الطلب | بيانات التوصيل + اختيار وسيلة الدفع |
| `/pay/[code]` | صفحة الدفع | ملخص الطلب + اختيار وسيلة الدفع + دفع |
| `/track` | تتبع الطلب | ادخل رمز ORD-/CST- + خط زمني للحالة |
| `/admin` | لوحة التحكم | **محمية بكلمة مرور** — إدارة كل شيء |
| `/admin/[tab]` | التبويبات (مدمجة) | التصاميس المخصصة / الطلبات / المنتجات / **إنستقرام** (تسعير المستورد ونشره) |

---

## 🔑 بيانات الدخول

| البيان | القيمة | مكان التحديث |
|---|---|---|
| **كلمة مرور اللوحة** | في `.env.local` (لا تُكتب هنا) | `.env.local` — غير مرفوع على git |
| **رقم الواتساب** | `0566123883` | `lib/format.ts` (WHATSAPP_NUMBER) |
| **حساب إنستقرام** | `@m3dstore2026` | `lib/format.ts` (INSTAGRAM_HANDLE) |
| **رسوم الشحن** | 25 ر.س | `lib/shipping.ts` (SHIPPING_FLAT) |
| **حد الشحن المجاني** | 200 ر.س | `lib/shipping.ts` (FREE_SHIPPING_OVER) |

---

## 📁 هيكل المجلدات

```
m3dstore/
├── app/                      # صفحات النكست.جس (App Router)
│   ├── page.tsx             # الرئيسية
│   ├── products/            # صفحة المنتجات وتفاصيل المنتج
│   ├── custom/              # صفحة الطلب المخصص
│   ├── cart/                # سلة المشتريات
│   ├── checkout/            # إتمام الطلب
│   ├── pay/                 # صفحة الدفع
│   ├── track/               # تتبع الطلب
│   ├── admin/               # لوحة التحكم
│   ├── api/                 # API endpoints
│   │   ├── orders/          # إنشاء طلبات الشراء
│   │   ├── custom/          # إنشاء طلبات التصاميم المخصصة
│   │   ├── track/           # تتبع الطلبات
│   │   ├── pay/             # معالجة الدفع
│   │   ├── admin/           # API اللوحة (محمية بكلمة مرور)
│   │   │   ├── login/       # دخول اللوحة
│   │   │   ├── data/        # جلب كل البيانات
│   │   │   ├── products/    # إدارة المنتجات
│   │   │   ├── orders/      # تحديث الطلبات
│   │   │   ├── custom/      # تحديث التصاميس المخصصة
│   │   │   └── file/        # تحميل الملفات المرفوعة
│   ├── layout.tsx           # الخطة العام (Header + Footer + Cart Provider)
│   ├── globals.css          # التصاميم العام + CSS Variables
│   └── not-found.tsx        # صفحة 404
├── components/
│   ├── Header.tsx           # الرأس + التنقل
│   ├── Footer.tsx           # التذييل
│   ├── ProductCard.tsx      # بطاقة المنتج
│   ├── AddToCart.tsx        # زر الإضافة للسلة
│   ├── PaymentPanel.tsx     # واجهة الدفع
│   ├── PaymentBadges.tsx    # شارات وسائل الدفع
│   ├── WhatsAppButton.tsx   # زر الواتساب العائم
│   ├── Icons.tsx            # 20+ أيقونة SVG مخصصة
│   └── cart.tsx             # Context للسلة
├── lib/
│   ├── db.ts                # قاعدة البيانات SQLite + البذر الأولي
│   ├── format.ts            # تنسيق الأسعار + الحالات + وسائل الدفع
│   ├── shipping.ts          # حساب الشحن
│   └── admin-auth.ts        # مصادقة اللوحة
├── public/
│   └── products/            # صور المنتجات (SVG مؤقتة)
├── data/                    # قاعدة البيانات (يُنشأ عند أول تشغيل)
│   ├── store.db             # SQLite database
│   └── uploads/             # الملفات المرفوعة من العملاء
├── .env.local               # المتغيرات البيئية (غير محمي — **أضف لـ .gitignore**)
├── next.config.ts           # إعدادات النكست
├── tailwind.config.ts       # إعدادات التايلويند
└── تشغيل المتجر.bat        # ملف بات سريع للتشغيل

```

---

## 🚀 التشغيل والنشر

### تشغيل محلي (التطوير)
```bash
npm run dev
# ثم افتح http://localhost:3000
```

أو اضغط مرتين على `تشغيل المتجر.bat` من الملفات.

### البناء للإنتاج
```bash
npm run build
npm run start
```

### النشر الحقيقي
1. **خادم مشغل** (Vercel، Render، DigitalOcean، إلخ)
2. **قاعدة بيانات** (قيد الاستخدام: SQLite محلي — انقل إلى PostgreSQL/MySQL للإنتاج)
3. **بوابة الدفع** (سجّل حساب تاجر في Moyasar أو Tap)
4. **نطاق اسم**

---

## 🔐 الأمان

**اهم نقاط**:
- كلمة مرور اللوحة في `.env.local` — **غيّرها فورًا**
- الجلسات محمية بـ HTTP-only cookies
- كل ملف مرفوع يُحفظ بمسار عشوائي (UUID)
- الدفع حاليًا تجريبي — تفعيل الحقيقي يتطلب مفاتيح API آمنة
- استخدم `.env.local` للمتغيرات الحساسة — **أضفها إلى .gitignore**

---

## 📊 قاعدة البيانات

**SQLite محلي** في `data/store.db` (ينشأ تلقائيًا عند أول تشغيل).

**الجداول**:
- `products` — المنتجات (8 منتجات تجريبية مدرجة)
- `orders` — طلبات الشراء (حالات: pending_payment, paid, processing, shipped, delivered, cancelled)
- `custom_requests` — طلبات التصاميس المخصصة (حالات: review, approved, paid, printing, shipped, delivered, rejected)

**البذر الأولي** (يحدث مرة واحدة):
```
INSERT INTO products (name, description, price, category, image, stock)
VALUES ('مزهرية هندسية عصرية', '...', 45, 'ديكورات وهدايا', '/products/vase.svg', 15),
       ('حامل جوال قابل للتعديل', '...', 25, 'قطع عملية وأدوات', '/products/phone-stand.svg', 30),
       ... (6 more)
```

**النسخ الاحتياطي**: انسخ مجلد `data/` بشكل دوري.

---

## 🎨 التخصيص

### تغيير الألوان
ملف `app/globals.css` — CSS Variables:
```css
:root {
  --primary: #7a6935;        /* الذهبي الزيتوني من الشعار */
  --primary-hover: #5c4e2a;  /* الذهبي الغامق للتحويم */
  --primary-soft: #fcf3e4;   /* الكريمي من الشعار */
  --accent: #8a5d30;         /* النحاسي للتأكيد */
  --background: #fbf8f2;     /* أبيض دافئ */
  ... (16 متغير آخر)
}
```

### تغيير الخط
ملف `app/layout.tsx` — استبدل خط Cairo بخط آخر:
```tsx
import { Cairo } from "next/font/google";
// أو اختر خط عربي آخر من Google Fonts
```

### إضافة صور حقيقية للمنتجات
1. ضع الصور في `public/products/` (JPG/PNG/WebP)
2. عند إضافة منتج من اللوحة، اختر الصورة من القائمة

### تغيير اللوقو
`components/Header.tsx` و `components/Footer.tsx`:
```tsx
<CubeIcon width={28} height={28} />  // ← استبدل بصورة اللوقو
```

---

## 📝 التعديلات الأخيرة (2026-07-19)

1. ✅ دقة الطباعة: تحديث من 0.1 ملم إلى **0.2 ملم** (`app/page.tsx`)
2. ✅ رفع الملف: **إجباري الآن** في `/custom` — الواجهة والـ API يرفضان الطلبات بدون ملف
3. ✅ لوحة التحكم: موثّقة بالكامل مع أمثلة الاستخدام

---

## 🔗 التكاملات المتاحة (جاهزة للربط)

| الخدمة | الملف | الحالة | الملاحظات |
|---|---|---|---|
| **Moyasar** | `components/PaymentPanel.tsx` | جاهزة | بوابة سعودية معتمدة — تدعم مدى + Apple Pay + STC Pay |
| **Tap** | `components/PaymentPanel.tsx` | جاهزة | بديل — تدعم وسائل متعددة |
| **Tabby** | `components/PaymentPanel.tsx` | جاهزة | تقسيط — يتطلب حساب تاجر منفصل |
| **Tamara** | `components/PaymentPanel.tsx` | جاهزة | تقسيط — يتطلب حساب تاجر منفصل |
| **STC Pay** | الواجهة فقط | جاهزة | المحفظة الرقمية السعودية |

**للتفعيل الحقيقي**:
1. سجّل في Moyasar (https://moyasar.com) — يحتاج سجل تجاري
2. ضع المفاتيح في `.env.local`:
   ```
   MOYASAR_PUBLISHABLE_KEY=pk_live_xxxxx
   MOYASAR_SECRET_KEY=sk_live_xxxxx
   ```
3. استبدل محاكي الدفع في `app/api/pay/route.ts` بـ Moyasar SDK

---

## ✨ الميزات المدمجة

- ✅ سلة تسوق دائمة (localStorage)
- ✅ حساب الشحن ديناميكي
- ✅ تتبع الطلبات برمز فريد
- ✅ رفع الملفات (حتى 40 MB)
- ✅ واجهة مسؤولة (جوال + تابليت + سطح مكتب)
- ✅ تجاهل الحركة (accessibility)
- ✅ نصوص عربية سلسة (RTL)
- ✅ أيقونات SVG مخصصة (20+ أيقونة)
- ✅ وضع تجريبي للدفع

---

## 🐛 الأخطاء المعروفة والحلول

| المشكلة | الحل |
|---|---|
| قاعدة البيانات تُنشأ في مجلد خاطئ | تحقق من `M3D_DATA_DIR` في `.env.local` — يجب أن يشير إلى مجلد `data` داخل المشروع |
| الصفحة بطيئة في أول تحميل | طبيعي — Next.js يجمّع الصفحة مرة واحدة. التحميلات التالية سريعة جدًا |
| الملفات المرفوعة لا تظهر في اللوحة | تأكد أن اسم الملف صحيح في قاعدة البيانات (`custom_requests.file_path`) |

---

## 📞 الدعم والصيانة

### نقاط الاتصال السريعة
- **رقم الواتساب المعروض**: `lib/format.ts` (WHATSAPP_DISPLAY)
- **رقم الواتساب الحقيقي**: `lib/format.ts` (WHATSAPP_NUMBER)

### الصيانة الدورية
1. **نسخ احتياطي أسبوعي** لمجلد `data/`
2. **مراجعة كلمات المرور** شهريًا
3. **تحديث المنتجات** من اللوحة
4. **حذف الطلبات المحتومة** (قديمة جدًا) يدويًا من قاعدة البيانات إذا لزم

---

## 🎯 الخطوات التالية

1. **غيّر كلمة المرور** في `.env.local`
2. **أضف صورك الحقيقية** للمنتجات في `public/products/`
3. **عدّل أسعار المنتجات** من اللوحة
4. **فعّل الدفع الحقيقي** — سجّل في Moyasar/Tap
5. **اختبر التدفق كاملاً**: `/custom` → طلب تجريبي → `/admin` → وافق → `/track` → ادفع
6. **نشّر على خادم حقيقي** (Vercel/Render/DigitalOcean)

---

## 📜 الملفات الهامة

| الملف | الغرض |
|---|---|
| `README.md` | دليل الاستخدام العام |
| `INSTAGRAM.md` | **استيراد صور إنستقرام كمنتجات** — الخطوات الكاملة |
| `.env.local` | المتغيرات البيئية (**غير محمي — أضفها لـ .gitignore**) |
| `lib/db.ts` | قاعدة البيانات + البذر الأولي |
| `app/layout.tsx` | الخطة الرئيسية (Header + Footer + Providers) |
| `app/admin/page.tsx` | كود لوحة التحكم الكامل (~550 سطر) |

---

## 🎓 ملاحظات للمطورين القادمين

- **Stack**: Next.js 16 (App Router) + Tailwind CSS + SQLite + TypeScript
- **لا توجد مكتبات UI ثقيلة** — كل الأنماط مبنية يدويًا بـ Tailwind (سهل التخصيص)
- **البيانات محلية 100%** — لا توجد APIs خارجية (ما عدا الدفع)
- **الملفات المرفوعة آمنة** — يتم حفظها برموز UUID عشوائية
- **الكود منظم وموثّق** — سهل الصيانة والتطوير

---

**آخر تحديث**: 19 يوليو 2026  
**الإصدار**: 1.0.0 (جاهز للإنتاج)  
**الحالة**: ✅ مكتمل وقيد التشغيل
