# 🚀 تشغيل سريع — M3DStore

## البدء الفوري (5 دقائق)

### 1️⃣ التشغيل
```bash
npm run dev
```
أو اضغط مرتين على `تشغيل المتجر.bat` من الملفات.

ثم افتح: **http://localhost:3000**

---

### 2️⃣ تجربة المتجر كعميل
| الخطوة | الرابط | ماذا تفعل |
|---|---|---|
| **تصفح المنتجات** | http://localhost:3000/products | شيك المنتجات — عندك 8 منتجات تجريبية |
| **شراء منتج** | اضغط على منتج → أضف للسلة | ادخل الكمية واضغط "اشترِ الآن" |
| **إتمام الطلب** | http://localhost:3000/checkout | ادخل اسمك وجوالك وعنوانك → اختر وسيلة الدفع → ادفع (تجريبي) |
| **تتبع الطلب** | http://localhost:3000/track | انسخ رمز الطلب من بعد الدفع → شيك الحالة |
| **طلب مخصص** | http://localhost:3000/custom | اشرح فكرتك + **رفع ملف (إجباري الآن)** |

---

### 3️⃣ الدخول لـ لوحة التحكم
**الرابط**: http://localhost:3000/admin

**كلمة المرور**: موجودة في ملف `.env.local` تحت `ADMIN_PASSWORD`

**ما تقدر تعمل من هناك**:
- ✅ **التصاميس المخصصة** — شيف طلب العميل → حمّل الملف → وافق بسعر (أو ارفض)
- ✅ **الطلبات** — شيف كل الطلبات → غيّر الحالة (دفع ← تجهيز ← شحن ← توصيل)
- ✅ **المنتجات** — ضيف منتج جديد / عدّل / اخفِ

---

## ⚡ الأوامر الأساسية

| الأمر | الغرض |
|---|---|
| `npm run dev` | تشغيل محلي (http://localhost:3000) |
| `npm run build` | بناء للإنتاج |
| `npm run start` | تشغيل نسخة الإنتاج |
| `npm run lint` | فحص الأخطاء |

---

## 🔧 إعدادات سريعة

### تغيير كلمة المرور اللوحة
**ملف**: `.env.local`
```
ADMIN_PASSWORD=كلمة_المرور_الجديدة
```

### تغيير رقم الواتساب
**ملف**: `lib/format.ts`
```typescript
export const WHATSAPP_NUMBER = "966501234567";
export const WHATSAPP_DISPLAY = "0501234567";
```

### تغيير رسوم الشحن
**ملف**: `lib/shipping.ts`
```typescript
export const SHIPPING_FLAT = 25;           // 25 ر.س
export const FREE_SHIPPING_OVER = 200;     // مجاني فوق 200 ر.س
```

---

## 📸 صور المنتجات

الصور الحالية رسومات مؤقتة (SVG). لإضافة صورك الحقيقية:

1. ضع الملفات (JPG/PNG) في `public/products/`
   ```
   public/products/
   ├── my-product-1.jpg
   ├── my-product-2.jpg
   └── ... (أي عدد)
   ```

2. من اللوحة، عند إضافة منتج جديد، اختر الصورة من القائمة

---

## 🗃️ قاعدة البيانات

كل بيانات المتجر محفوظة محليًا:
```
data/
├── store.db          # ← قاعدة البيانات (SQLite)
└── uploads/          # ← الملفات المرفوعة من العملاء
```

**النسخ الاحتياطي**: انسخ مجلد `data/` كاملاً بانتظام.

---

## 🔑 كلمات المرور والمفاتيح

| البيان | القيمة | ملف |
|---|---|---|
| كلمة مرور اللوحة | في `.env.local` (لا تُكتب في التوثيق) | `.env.local` |
| رقم واتساب | `0566123883` | `lib/format.ts` |

**⚠️ قبل النشر الحقيقي**:
- غيّر كلمة المرور
- أضف مفاتيح Moyasar إذا فعّلت الدفع الحقيقي
- ضع `.env.local` في `.gitignore`

---

## 🧪 اختبار كامل الدفق

1. **طلب عادي**:
   - افتح `/products` → اختر منتج → سلة → دفع تجريبي
   - من اللوحة `/admin` → الطلبات → شيف الطلب الجديد

2. **طلب مخصص**:
   - افتح `/custom` → ادخل بيانات + **رفع ملف**
   - من اللوحة `/admin` → التصاميس المخصصة → وافق بسعر
   - من العميل: `/track` → ادفع
   - من اللوحة: غيّر الحالة (ابدأ طباعة ← شحن ← توصيل)

---

## 📱 الصفحات المهمة

| الصفحة | الرابط | من يستخدمها |
|---|---|---|
| الرئيسية | `/` | العملاء |
| المنتجات | `/products` | العملاء |
| طلب مخصص | `/custom` | العملاء |
| تتبع الطلب | `/track` | العملاء |
| السلة والدفع | `/cart`, `/checkout`, `/pay/[code]` | العملاء |
| لوحة التحكم | `/admin` | أنت فقط (محمية) |

---

## ⚙️ التشغيل على الإنتاج

### قبل النشر
1. ✅ غيّر كلمة المرور
2. ✅ فعّل الدفع الحقيقي (Moyasar/Tap)
3. ✅ أضف صورك الحقيقية
4. ✅ اختبر كل الدفق
5. ✅ ضع `.env.local` في `.gitignore`

### النشر (مثال على Vercel)
```bash
git add .
git commit -m "Ready for production"
git push origin main
# Vercel تحصل على التحديث تلقائيًا
```

---

## 🆘 حل الأخطاء الشائعة

| المشكلة | الحل |
|---|---|
| **"Cannot find module 'better-sqlite3'"** | `npm install` ثم `npm run dev` من جديد |
| **"M3D_DATA_DIR: No such directory"** | تحقق من المسار في `.env.local` — يجب أن يكون مسار موجود |
| **الصفحة بيضاء** | افتح console (F12) وشيف الأخطاء — غالبًا مشكلة في قاعدة البيانات |
| **الدفع لا يعمل** | تأكد أن الدفع تجريبي فقط — للحقيقي ستحتاج Moyasar API |

---

**الآن أنت جاهز للبدء! 🎉**

لأي أسئلة تفصيلية، اقرأ [HANDOVER.md](HANDOVER.md) أو [README.md](README.md).
